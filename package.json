{
  "name": "multidomain-server",
  "description": "a webserver that can handle multiple domains, and custom endpoints per domain",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
